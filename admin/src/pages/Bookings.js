import React from 'react';
import { withRouter } from 'react-router-dom';

function Bookings() {
    return (
        <div>
            <h1>Bookings Page</h1>
        </div>
    );
}

export default withRouter(Bookings);
